const INITIAL_STATE = {
  nome: 'Helder Daniel Silva Araujo' 
};

const initialReducer = (state = INITIAL_STATE, action) => {
 switch(action.type) {
   default:
    return state;
 }
}

export default initialReducer;
